class AiEngine
{
public:
	AiEngine(void);
	std::vector<aiPerson*> npc;
	std::vector<aiEvent*> events;
	std::vector<aiRelation*> relations;
	std::vector<aiPerson*> queuedNpc;
	std::vector<aiEvent*> queuedEvents;
	std::vector<aiRelation*> queuedRelations;
	std::vector<Events*> getAllEvents();
	std::list<aiPerson*> generateAi();
	int populate(int population);
	~AiEngine(void);
};


Person:
	Personality, Age, Race, Occupation, 
	Schedule, CurrentActivity, 